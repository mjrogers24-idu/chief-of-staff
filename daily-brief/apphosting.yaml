# Firebase App Hosting config for the daily-brief Next.js app.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  minInstances: 0
  maxInstances: 1
  concurrency: 80
  cpu: 1
  memoryMiB: 512

env:
  # Public Firebase Web SDK config — safe as plain values (same reasoning
  # as .env.local.example: this is exposed to the browser by design;
  # Firestore/Storage rules are the real security boundary, not this key).
  - variable: NEXT_PUBLIC_FIREBASE_API_KEY
    value: AIzaSyDAAoy6IIfPVq40SC4OpVqgq6lngwo4PAM
    availability: [BUILD, RUNTIME]
  - variable: NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN
    value: daily-brief-b4383.firebaseapp.com
    availability: [BUILD, RUNTIME]
  - variable: NEXT_PUBLIC_FIREBASE_PROJECT_ID
    value: daily-brief-b4383
    availability: [BUILD, RUNTIME]
  - variable: NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET
    value: daily-brief-b4383.firebasestorage.app
    availability: [BUILD, RUNTIME]
  - variable: NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID
    value: "1052531140945"
    availability: [BUILD, RUNTIME]
  - variable: NEXT_PUBLIC_FIREBASE_APP_ID
    value: "1:1052531140945:web:30bb8471780c57d1cf0b70"
    availability: [BUILD, RUNTIME]
  # "Web push certificate" key pair for browser push notifications —
  # Firebase console > Project settings > Cloud Messaging > Web Push
  # certificates > Generate key pair. Needed at BUILD time (Next.js inlines
  # NEXT_PUBLIC_ vars into the client bundle), so notifications stay
  # disabled ("misconfigured") until this is filled in and redeployed.
  - variable: NEXT_PUBLIC_FIREBASE_VAPID_KEY
    value: "BITrmX1rKkTcg3eDJgU-Dh6bffXljwM55qaKLLNKJlZeB6cOxpc7KXgJBHDNiDijGJnPCfDwRSrGyBb8mhl7Yxs"
    availability: [BUILD, RUNTIME]

  # Backend "chief-of-staff", assigned once the App Hosting backend was
  # created. "<this>/api/google/callback" also needs to be added to the
  # OAuth client's authorized redirect URIs in Google Cloud Console.
  - variable: GOOGLE_OAUTH_REDIRECT_BASE_URL
    value: "https://chief-of-staff--daily-brief-b4383.us-east4.hosted.app"
    availability: [RUNTIME]

  # Secrets — stored in Google Secret Manager, referenced by name here.
  # Create each with: firebase apphosting:secrets:set <NAME>
  # FIREBASE_SERVICE_ACCOUNT_KEY rotated 2026-08-07 to pick up a fresh,
  # correctly-pasted key (secrets are pinned at deploy time, so this
  # comment exists just to trigger a new rollout).
  - variable: FIREBASE_SERVICE_ACCOUNT_KEY
    secret: FIREBASE_SERVICE_ACCOUNT_KEY
    availability: [RUNTIME]
  - variable: GOOGLE_OAUTH_CLIENT_ID
    secret: GOOGLE_OAUTH_CLIENT_ID
    availability: [RUNTIME]
  - variable: GOOGLE_OAUTH_CLIENT_SECRET
    secret: GOOGLE_OAUTH_CLIENT_SECRET
    availability: [RUNTIME]
  - variable: OAUTH_STATE_SECRET
    secret: OAUTH_STATE_SECRET
    availability: [RUNTIME]
